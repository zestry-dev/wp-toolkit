<?php

/**
 * DevTools: sample values for `stubs/bootstrap.php.stub`.
 */

declare( strict_types=1 );

/*
 * The plugin's own loaded action, which `init` fills in from the slug it is
 * writing for. Spelled here as the sample plugin's, so the file explains
 * headings using a hook name that is real rather than a placeholder.
 */
return array(
	'{{loaded_hook}}' => 'acme_plugin_loaded',
);
